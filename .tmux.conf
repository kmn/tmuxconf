unbind C-b
set -g prefix ^A
bind a send-prefix

bind [ copy-mode -u
setw -g mode-keys vi

bind | split-window -h
bind - split-window -v

bind j select-pane -D
bind k select-pane -U
bind h select-pane -L
bind l select-pane -R

